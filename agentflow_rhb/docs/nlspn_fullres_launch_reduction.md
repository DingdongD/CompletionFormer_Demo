# NLSPN Full-resolution Launch Reduction Probe, 2026-07-14

Input: NYU sample0, 128x128.

## Candidate 1: Full Conv / Conv+BN+ReLU

Generated full compiler-aligned candidates:

```text
nlspn_ckpt_dec2_full_bn_relu
nlspn_ckpt_id_dec1_full_bn_relu
nlspn_ckpt_gd_dec1_full_bn_relu
nlspn_ckpt_cf_dec1_full_bn_relu
nlspn_ckpt_pred_init_full_relu
nlspn_ckpt_guidance_full
```

Result:

```text
export=0 compile=0 cmodel=0 pack=0 board_all_same=False for all six
```

Conclusion: full 128x128 conv candidates are not board-exact even though they compile and pass CModel. They must not replace the accepted 64-channel split path.

Evidence:

```text
/root/demo/artifacts/rhb_auto_config_framework/reports/nlspn_fullres_fused_probe_20260714.tsv
/root/demo/artifacts/rhb_auto_config_framework/reports/nlspn_fullres_fused_probe_20260714_logs/
```

## Candidate 2: 128-channel Input Chunk

Generated medium-fusion candidates:

```text
nlspn_ckpt_dec2_partial128_oc0_64_ic0_128
nlspn_ckpt_id_dec1_partial128_oc0_64_ic0_128
nlspn_ckpt_gd_dec1_partial128_oc0_64_ic0_128
nlspn_ckpt_cf_dec1_partial128_oc0_32_ic0_128
nlspn_ckpt_pred_init_partial128_oc0_1_ic0_128
nlspn_ckpt_guidance_partial128_oc0_8_ic0_128
```

Result:

```text
export=0 compile=0 cmodel=0 pack=0 board_all_same=False for all six
```

Conclusion: for strict board-exact deployment at 128x128, input-channel chunks should stay at 64 channels. Raising the input chunk to 128 breaks board exactness.

Evidence:

```text
/root/demo/artifacts/rhb_auto_config_framework/reports/nlspn_fullres_ic128_probe_20260714.tsv
/root/demo/artifacts/rhb_auto_config_framework/reports/nlspn_fullres_ic128_probe_20260714_logs/
```

## Candidate 3: Host Post Affine

The runner now supports optional Host-side post affine:

```text
--host-affines-npz <npz>
--host-post-prefix <prefix>
```

The affine npz is generated by:

```text
/root/demo/artifacts/rhb_auto_config_framework/scripts/export_nlspn_host_affines.py
```

### All Full-res Post on Host

Tested Host post for:

```text
dec2 id_dec1 gd_dec1 cf_dec1 pred_init guidance
```

Result:

```text
LATENCY_TOTAL_MS 3485.621 ms
pred mean_abs vs ref 0.709279
```

This is faster but not numerically acceptable. Direct Host BN/ReLU does not reproduce the effective board quantization contract of the RHB post submodels.

Scale-aware qdq Host post was also tested:

```text
LATENCY_TOTAL_MS 3901.440 ms
pred mean_abs vs ref 0.730331
```

This is still not acceptable.

### Head Bias Post Only on Host

Tested Host post only for:

```text
pred_init guidance
```

Result:

```text
LATENCY_TOTAL_MS 4018.124 ms
pred mean_abs vs ref 0.136192
pred rmse vs ref     0.174921
```

Compared with accepted profiled baseline:

```text
LATENCY_TOTAL_MS 4388.236 ms
pred mean_abs vs ref 0.136143
pred rmse vs ref     0.174810
```

Conclusion: `pred_init` and `guidance` bias post can be moved to Host with negligible output drift on sample0. This removes two RHB launches. It should be validated on val32 before becoming the default runner path.

Evidence:

```text
/root/demo/artifacts/rhb_auto_config_framework/reports/nlspn_final_board_pipeline_sample0_hostpost_heads_qdq_20260714.log
/root/demo/artifacts/rhb_auto_config_framework/work/nlspn_board_final_sample0/nlspn_board_sample0_outputs_hostpost_heads_qdq_20260714.npz
```

## Current Recommendation

For strict board-exact / near-exact NLSPN deployment:

1. Keep 64-channel input chunks for 128x128 convs.
2. Do not use full fused 128x128 conv submodels.
3. Do not move BN/ReLU post blocks to Host without an effective affine calibration flow.
4. Consider adopting Host post only for `pred_init` and `guidance` after val32 validation.
5. Larger latency wins require a retrained compiler-aligned architecture that reduces the number of full-resolution 3x3 conv branches, not just larger exact subgraphs.
